logging:
    level:
        org.sav: INFO

spring:
    config:
        import:
            - optional:configserver:${config.server.url}
    cloud:
        vault:
            enabled: false
        stream:
            kafka:
                binder:
                    brokers: none